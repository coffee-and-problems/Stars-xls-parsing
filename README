Приложение написано на .NET Core и для работы требует его установки.
Найти инструкцию по установке на системы Windows, Linux, macOS можно здесь: https://www.microsoft.com/net/download

Приложение ищет папку data в каталоге с файлами .json. В ней - файлы ListOfStars.csv, 2mass.csv, apass.csv, gaia.csv, nomad.csv, ucac4.csv и 2mass.csv.
Если какой-то из файлов отсутствует, программа дополнит соответствующие данные нулями. Регист не важен (gaia.csv и GaiA.csv воспринимаются как один файл).

Для каждого csv-файла считается, что первая строка содержит ключи (названия столбцов) в кодировке ASCII или Unicode или в бинарном виде. Вторая строка отводится под комментарий. Поля не должны содержать символов перевода строки.

Загрузка и поиск данных производится по названию столбца. Поэтому порядок и количество столбцов не важен, однако столбцы, содержащие пару значение-погрешность должны идти подряд.